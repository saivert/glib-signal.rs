[package]
name = "examples"
version = "0.0.0"
authors = ["arcnmx"]
edition = "2018"

[dependencies]
glib-signal = { version = "0.1", features = ["futures"], path = "../" }
futures = "0.3"
glib = { version = "0.16" }
ctrlc = { version = "3.0", features = ["termination"] }
